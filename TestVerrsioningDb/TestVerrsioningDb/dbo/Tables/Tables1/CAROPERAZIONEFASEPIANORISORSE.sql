CREATE TABLE [dbo].[CAROPERAZIONEFASEPIANORISORSE] (
    [NOMEPIANORIS]   VARCHAR (10)  NOT NULL,
    [PROGRESSIVO]    DECIMAL (10)  NOT NULL,
    [NUMEROFASE]     INT           NOT NULL,
    [POSIZIONE]      INT           NOT NULL,
    [VALORE]         VARCHAR (100) NULL,
    [UTENTEMODIFICA] VARCHAR (25)  NOT NULL,
    [DATAMODIFICA]   DATETIME      NOT NULL,
    CONSTRAINT [PK_CAROPERAZIONEFASEPIANORISOR] PRIMARY KEY CLUSTERED ([NOMEPIANORIS] ASC, [PROGRESSIVO] ASC, [NUMEROFASE] ASC, [POSIZIONE] ASC) WITH (FILLFACTOR = 90)
);


GO


/*  INSERT TRIGGER "TI_CAROPERAZIONEFASEPIANORISOR" FOR TABLE "CAROPERAZIONEFASEPIANORISORSE"  */
CREATE TRIGGER TI_CAROPERAZIONEFASEPIANORISOR ON CAROPERAZIONEFASEPIANORISORSE FOR INSERT AS
BEGIN
    DECLARE
       @MAXCARD  INT,
       @NUMROWS  INT,
       @NUMNULL  INT,
       @ERRNO    INT,
       @ERRMSG   VARCHAR(255)

    SELECT  @NUMROWS = @@ROWCOUNT
    IF @NUMROWS = 0
       RETURN

    
    /*  PARENT "RIGHECICLOPIANORISORSE" MUST EXIST WHEN INSERTING A CHILD IN "CAROPERAZIONEFASEPIANORISORSE"  */
    IF UPDATE(NOMEPIANORIS) OR
       UPDATE(PROGRESSIVO) OR
       UPDATE(NUMEROFASE)
    BEGIN
       IF (SELECT COUNT(*)
           FROM   RIGHECICLOPIANORISORSE T1, INSERTED T2
           WHERE  T1.NOMEPIANORIS = T2.NOMEPIANORIS
            AND   T1.PROGRESSIVO = T2.PROGRESSIVO
            AND   T1.NUMEROFASE = T2.NUMEROFASE) != @NUMROWS
          BEGIN
             SELECT @ERRNO  = 30002,
                    @ERRMSG = 'Parent does not exist in "RIGHECICLOPIANORISORSE". Cannot create child in "CAROPERAZIONEFASEPIANORISORSE".'
             GOTO ERROR
          END
    END

    RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO

/*  UPDATE TRIGGER "TU_CAROPERAZIONEFASEPIANORISOR" FOR TABLE "CAROPERAZIONEFASEPIANORISORSE"  */
CREATE TRIGGER TU_CAROPERAZIONEFASEPIANORISOR ON CAROPERAZIONEFASEPIANORISORSE FOR UPDATE AS
BEGIN
   DECLARE
      @MAXCARD  INT,
      @NUMROWS  INT,
      @NUMNULL  INT,
      @ERRNO    INT,
      @ERRMSG   VARCHAR(255)

      SELECT  @NUMROWS = @@ROWCOUNT
      IF @NUMROWS = 0
         RETURN

      
      /*  PARENT "RIGHECICLOPIANORISORSE" MUST EXIST WHEN UPDATING A CHILD IN "CAROPERAZIONEFASEPIANORISORSE"  */
      IF UPDATE(NOMEPIANORIS) OR
         UPDATE(PROGRESSIVO) OR
         UPDATE(NUMEROFASE)
      BEGIN
         IF (SELECT COUNT(*)
             FROM   RIGHECICLOPIANORISORSE T1, INSERTED T2
             WHERE  T1.NOMEPIANORIS = T2.NOMEPIANORIS
              AND   T1.PROGRESSIVO = T2.PROGRESSIVO
              AND   T1.NUMEROFASE = T2.NUMEROFASE) != @NUMROWS
            BEGIN
               SELECT @ERRNO  = 30003,
                      @ERRMSG = '"RIGHECICLOPIANORISORSE" does not exist. Cannot modify child in "CAROPERAZIONEFASEPIANORISORSE".'
               GOTO ERROR
            END
      END

      RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO
GRANT DELETE
    ON OBJECT::[dbo].[CAROPERAZIONEFASEPIANORISORSE] TO [Metodo98]
    AS [dbo];


GO
GRANT INSERT
    ON OBJECT::[dbo].[CAROPERAZIONEFASEPIANORISORSE] TO [Metodo98]
    AS [dbo];


GO
GRANT REFERENCES
    ON OBJECT::[dbo].[CAROPERAZIONEFASEPIANORISORSE] TO [Metodo98]
    AS [dbo];


GO
GRANT SELECT
    ON OBJECT::[dbo].[CAROPERAZIONEFASEPIANORISORSE] TO [Metodo98]
    AS [dbo];


GO
GRANT UPDATE
    ON OBJECT::[dbo].[CAROPERAZIONEFASEPIANORISORSE] TO [Metodo98]
    AS [dbo];

