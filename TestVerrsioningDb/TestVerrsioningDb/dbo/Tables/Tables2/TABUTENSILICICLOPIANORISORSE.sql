CREATE TABLE [dbo].[TABUTENSILICICLOPIANORISORSE] (
    [NOMEPIANORIS]   VARCHAR (10) NOT NULL,
    [PROGRESSIVO]    DECIMAL (10) NOT NULL,
    [NUMEROFASE]     INT          NOT NULL,
    [POSIZIONE]      INT          NOT NULL,
    [CODICEUTENSILE] VARCHAR (15) NULL,
    [UTENTEMODIFICA] VARCHAR (25) NOT NULL,
    [DATAMODIFICA]   DATETIME     NOT NULL,
    CONSTRAINT [PK_TABUTENSILICICLOPIANORISORS] PRIMARY KEY CLUSTERED ([NOMEPIANORIS] ASC, [PROGRESSIVO] ASC, [NUMEROFASE] ASC, [POSIZIONE] ASC) WITH (FILLFACTOR = 90)
);


GO

/*  INSERT TRIGGER "TI_TABUTENSILICICLOPIANORISORS" FOR TABLE "TABUTENSILICICLOPIANORISORSE"  */
CREATE TRIGGER TI_TABUTENSILICICLOPIANORISORS ON TABUTENSILICICLOPIANORISORSE FOR INSERT AS
BEGIN
    DECLARE
       @MAXCARD  INT,
       @NUMROWS  INT,
       @NUMNULL  INT,
       @ERRNO    INT,
       @ERRMSG   VARCHAR(255)

    SELECT  @NUMROWS = @@ROWCOUNT
    IF @NUMROWS = 0
       RETURN

    
    /*  PARENT "RIGHECICLOPIANORISORSE" MUST EXIST WHEN INSERTING A CHILD IN "TABUTENSILICICLOPIANORISORSE"  */
    IF UPDATE(NOMEPIANORIS) OR
       UPDATE(PROGRESSIVO) OR
       UPDATE(NUMEROFASE)
    BEGIN
       IF (SELECT COUNT(*)
           FROM   RIGHECICLOPIANORISORSE T1, INSERTED T2
           WHERE  T1.NOMEPIANORIS = T2.NOMEPIANORIS
            AND   T1.PROGRESSIVO = T2.PROGRESSIVO
            AND   T1.NUMEROFASE = T2.NUMEROFASE) != @NUMROWS
          BEGIN
             SELECT @ERRNO  = 30002,
                    @ERRMSG = 'Parent does not exist in "RIGHECICLOPIANORISORSE". Cannot create child in "TABUTENSILICICLOPIANORISORSE".'
             GOTO ERROR
          END
    END

    RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO

/*  UPDATE TRIGGER "TU_TABUTENSILICICLOPIANORISORS" FOR TABLE "TABUTENSILICICLOPIANORISORSE"  */
CREATE TRIGGER TU_TABUTENSILICICLOPIANORISORS ON TABUTENSILICICLOPIANORISORSE FOR UPDATE AS
BEGIN
   DECLARE
      @MAXCARD  INT,
      @NUMROWS  INT,
      @NUMNULL  INT,
      @ERRNO    INT,
      @ERRMSG   VARCHAR(255)

      SELECT  @NUMROWS = @@ROWCOUNT
      IF @NUMROWS = 0
         RETURN

      
      /*  PARENT "RIGHECICLOPIANORISORSE" MUST EXIST WHEN UPDATING A CHILD IN "TABUTENSILICICLOPIANORISORSE"  */
      IF UPDATE(NOMEPIANORIS) OR
         UPDATE(PROGRESSIVO) OR
         UPDATE(NUMEROFASE)
      BEGIN
         IF (SELECT COUNT(*)
             FROM   RIGHECICLOPIANORISORSE T1, INSERTED T2
             WHERE  T1.NOMEPIANORIS = T2.NOMEPIANORIS
              AND   T1.PROGRESSIVO = T2.PROGRESSIVO
              AND   T1.NUMEROFASE = T2.NUMEROFASE) != @NUMROWS
            BEGIN
               SELECT @ERRNO  = 30003,
                      @ERRMSG = '"RIGHECICLOPIANORISORSE" does not exist. Cannot modify child in "TABUTENSILICICLOPIANORISORSE".'
               GOTO ERROR
            END
      END

      RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO
GRANT DELETE
    ON OBJECT::[dbo].[TABUTENSILICICLOPIANORISORSE] TO [Metodo98]
    AS [dbo];


GO
GRANT INSERT
    ON OBJECT::[dbo].[TABUTENSILICICLOPIANORISORSE] TO [Metodo98]
    AS [dbo];


GO
GRANT REFERENCES
    ON OBJECT::[dbo].[TABUTENSILICICLOPIANORISORSE] TO [Metodo98]
    AS [dbo];


GO
GRANT SELECT
    ON OBJECT::[dbo].[TABUTENSILICICLOPIANORISORSE] TO [Metodo98]
    AS [dbo];


GO
GRANT UPDATE
    ON OBJECT::[dbo].[TABUTENSILICICLOPIANORISORSE] TO [Metodo98]
    AS [dbo];

