create view VISTACOMPONENTIFASE AS
	select 
		TCL.PROGRESSIVO,IMP.NUMEROFASE,TCL.IDTESTACOMM,TCL.IDRIGACOMM,IMP.IDRIGADBA
	from 
		TABCOMPONENTIDBACICLOORD IMP inner join TESTACICLOORDINE TCL
		on IMP.PROGRESSIVO=TCL.PROGRESSIVO

GO
GRANT SELECT
    ON OBJECT::[dbo].[VISTACOMPONENTIFASE] TO [Metodo98]
    AS [dbo];

