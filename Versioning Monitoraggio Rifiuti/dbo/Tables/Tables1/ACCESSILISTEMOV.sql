CREATE TABLE [dbo].[ACCESSILISTEMOV] (
    [CODLISTA]       VARCHAR (3)  NOT NULL,
    [NOMEUTENTE]     VARCHAR (25) NOT NULL,
    [TIPORIGHEORD]   VARCHAR (3)  NULL,
    [TIPORIGHEIMP]   VARCHAR (3)  NULL,
    [TIPOPIEDE]      VARCHAR (3)  NULL,
    [FLAGABILITA]    SMALLINT     DEFAULT (0) NULL,
    [UTENTEMODIFICA] VARCHAR (25) NOT NULL,
    [DATAMODIFICA]   DATETIME     NOT NULL,
    CONSTRAINT [PK_ACCESSILISTEMOV] PRIMARY KEY CLUSTERED ([CODLISTA] ASC, [NOMEUTENTE] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [CKC_FLAGABILITA_ACCESSIL] CHECK ([FLAGABILITA] = 0 or [FLAGABILITA] = 1)
);


GO

/*  INSERT TRIGGER "TI_ACCESSILISTEMOV" FOR TABLE "ACCESSILISTEMOV"  */
CREATE TRIGGER TI_ACCESSILISTEMOV ON ACCESSILISTEMOV FOR INSERT AS
BEGIN
    DECLARE
       @MAXCARD  INT,
       @NUMROWS  INT,
       @NUMNULL  INT,
       @ERRNO    INT,
       @ERRMSG   VARCHAR(255)

    SELECT  @NUMROWS = @@ROWCOUNT
    IF @NUMROWS = 0
       RETURN

    
    /*  PARENT "PARAMETRILISTEPROD" MUST EXIST WHEN INSERTING A CHILD IN "ACCESSILISTEMOV"  */
    IF UPDATE(CODLISTA)
    BEGIN
       IF (SELECT COUNT(*)
           FROM   PARAMETRILISTEPROD T1, INSERTED T2
           WHERE  T1.CODICE = T2.CODLISTA) != @NUMROWS
          BEGIN
             SELECT @ERRNO  = 30002,
                    @ERRMSG = 'Parent does not exist in "PARAMETRILISTEPROD". Cannot create child in "ACCESSILISTEMOV".'
             GOTO ERROR
          END
    END

    RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO

/*  UPDATE TRIGGER "TU_ACCESSILISTEMOV" FOR TABLE "ACCESSILISTEMOV"  */
CREATE TRIGGER TU_ACCESSILISTEMOV ON ACCESSILISTEMOV FOR UPDATE AS
BEGIN
   DECLARE
      @MAXCARD  INT,
      @NUMROWS  INT,
      @NUMNULL  INT,
      @ERRNO    INT,
      @ERRMSG   VARCHAR(255)

      SELECT  @NUMROWS = @@ROWCOUNT
      IF @NUMROWS = 0
         RETURN

      
      /*  PARENT "PARAMETRILISTEPROD" MUST EXIST WHEN UPDATING A CHILD IN "ACCESSILISTEMOV"  */
      IF UPDATE(CODLISTA)
      BEGIN
         IF (SELECT COUNT(*)
             FROM   PARAMETRILISTEPROD T1, INSERTED T2
             WHERE  T1.CODICE = T2.CODLISTA) != @NUMROWS
            BEGIN
               SELECT @ERRNO  = 30003,
                      @ERRMSG = '"PARAMETRILISTEPROD" does not exist. Cannot ALTER COLUMN child in "ACCESSILISTEMOV".'
               GOTO ERROR
            END
      END

      RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO
GRANT DELETE
    ON OBJECT::[dbo].[ACCESSILISTEMOV] TO [Metodo98]
    AS [dbo];


GO
GRANT INSERT
    ON OBJECT::[dbo].[ACCESSILISTEMOV] TO [Metodo98]
    AS [dbo];


GO
GRANT REFERENCES
    ON OBJECT::[dbo].[ACCESSILISTEMOV] TO [Metodo98]
    AS [dbo];


GO
GRANT SELECT
    ON OBJECT::[dbo].[ACCESSILISTEMOV] TO [Metodo98]
    AS [dbo];


GO
GRANT UPDATE
    ON OBJECT::[dbo].[ACCESSILISTEMOV] TO [Metodo98]
    AS [dbo];

