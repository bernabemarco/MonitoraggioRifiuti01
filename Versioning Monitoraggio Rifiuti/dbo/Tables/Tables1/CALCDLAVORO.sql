CREATE TABLE [dbo].[CALCDLAVORO] (
    [CODCDL]         VARCHAR (5)    NOT NULL,
    [ANNOCAL]        DECIMAL (4)    NOT NULL,
    [MESECAL]        DECIMAL (2)    NOT NULL,
    [GIORNOCAL]      DECIMAL (2)    NOT NULL,
    [DATACAL]        DATETIME       NULL,
    [FESTIVO]        SMALLINT       DEFAULT (0) NULL,
    [ORELAV]         DECIMAL (6, 4) NULL,
    [DAORA1T]        DATETIME       NULL,
    [AORA1T]         DATETIME       NULL,
    [SOSTA1T]        DATETIME       NULL,
    [DAORA2T]        DATETIME       NULL,
    [AORA2T]         DATETIME       NULL,
    [SOSTA2T]        DATETIME       NULL,
    [DAORA3T]        DATETIME       NULL,
    [AORA3T]         DATETIME       NULL,
    [SOSTA3T]        DATETIME       NULL,
    [DAORA4T]        DATETIME       NULL,
    [AORA4T]         DATETIME       NULL,
    [SOSTA4T]        DATETIME       NULL,
    [CODDITTA]       VARCHAR (8)    NULL,
    [UTENTEMODIFICA] VARCHAR (25)   NOT NULL,
    [DATAMODIFICA]   DATETIME       NOT NULL,
    CONSTRAINT [PK_CALCDLAVORO] PRIMARY KEY CLUSTERED ([CODCDL] ASC, [ANNOCAL] ASC, [MESECAL] ASC, [GIORNOCAL] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [CKC_FESTIVO_CALCDLAV] CHECK ([FESTIVO] = 0 or [FESTIVO] = 1)
);


GO

/*  INSERT TRIGGER "TI_CALCDLAVORO" FOR TABLE "CALCDLAVORO"  */
CREATE TRIGGER TI_CALCDLAVORO ON CALCDLAVORO FOR INSERT AS
BEGIN
    DECLARE
       @MAXCARD  INT,
       @NUMROWS  INT,
       @NUMNULL  INT,
       @ERRNO    INT,
       @ERRMSG   VARCHAR(255)

    SELECT  @NUMROWS = @@ROWCOUNT
    IF @NUMROWS = 0
       RETURN

    
    /*  PARENT "TABELLACDLAVORO" MUST EXIST WHEN INSERTING A CHILD IN "CALCDLAVORO"  */
    IF UPDATE(CODCDL)
    BEGIN
       IF (SELECT COUNT(*)
           FROM   TABELLACDLAVORO T1, INSERTED T2
           WHERE  T1.CODICE = T2.CODCDL) != @NUMROWS
          BEGIN
             SELECT @ERRNO  = 30002,
                    @ERRMSG = 'Parent does not exist in "TABELLACDLAVORO". Cannot create child in "CALCDLAVORO".'
             GOTO ERROR
          END
    END

    RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO

/*  UPDATE TRIGGER "TU_CALCDLAVORO" FOR TABLE "CALCDLAVORO"  */
CREATE TRIGGER TU_CALCDLAVORO ON CALCDLAVORO FOR UPDATE AS
BEGIN
   DECLARE
      @MAXCARD  INT,
      @NUMROWS  INT,
      @NUMNULL  INT,
      @ERRNO    INT,
      @ERRMSG   VARCHAR(255)

      SELECT  @NUMROWS = @@ROWCOUNT
      IF @NUMROWS = 0
         RETURN

      
      /*  PARENT "TABELLACDLAVORO" MUST EXIST WHEN UPDATING A CHILD IN "CALCDLAVORO"  */
      IF UPDATE(CODCDL)
      BEGIN
         IF (SELECT COUNT(*)
             FROM   TABELLACDLAVORO T1, INSERTED T2
             WHERE  T1.CODICE = T2.CODCDL) != @NUMROWS
            BEGIN
               SELECT @ERRNO  = 30003,
                      @ERRMSG = '"TABELLACDLAVORO" does not exist. Cannot ALTER COLUMN child in "CALCDLAVORO".'
               GOTO ERROR
            END
      END

      RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO
GRANT DELETE
    ON OBJECT::[dbo].[CALCDLAVORO] TO [Metodo98]
    AS [dbo];


GO
GRANT INSERT
    ON OBJECT::[dbo].[CALCDLAVORO] TO [Metodo98]
    AS [dbo];


GO
GRANT REFERENCES
    ON OBJECT::[dbo].[CALCDLAVORO] TO [Metodo98]
    AS [dbo];


GO
GRANT SELECT
    ON OBJECT::[dbo].[CALCDLAVORO] TO [Metodo98]
    AS [dbo];


GO
GRANT UPDATE
    ON OBJECT::[dbo].[CALCDLAVORO] TO [Metodo98]
    AS [dbo];

