CREATE TABLE [dbo].[CALMACCHINE] (
    [CODMAC]         VARCHAR (5)    NOT NULL,
    [ANNOCAL]        DECIMAL (4)    NOT NULL,
    [MESECAL]        DECIMAL (2)    NOT NULL,
    [GIORNOCAL]      DECIMAL (2)    NOT NULL,
    [DATACAL]        DATETIME       NULL,
    [FESTIVO]        SMALLINT       DEFAULT (0) NULL,
    [ORELAV]         DECIMAL (6, 4) DEFAULT (0) NULL,
    [DAORA1T]        DATETIME       NULL,
    [AORA1T]         DATETIME       NULL,
    [SOSTA1T]        DATETIME       NULL,
    [DAORA2T]        DATETIME       NULL,
    [AORA2T]         DATETIME       NULL,
    [SOSTA2T]        DATETIME       NULL,
    [DAORA3T]        DATETIME       NULL,
    [AORA3T]         DATETIME       NULL,
    [SOSTA3T]        DATETIME       NULL,
    [DAORA4T]        DATETIME       NULL,
    [AORA4T]         DATETIME       NULL,
    [SOSTA4T]        DATETIME       NULL,
    [UTENTEMODIFICA] VARCHAR (25)   NOT NULL,
    [DATAMODIFICA]   DATETIME       NOT NULL,
    CONSTRAINT [PK_CALMACCHINE] PRIMARY KEY CLUSTERED ([CODMAC] ASC, [ANNOCAL] ASC, [MESECAL] ASC, [GIORNOCAL] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [CKC_FESTIVO_CALMACCH] CHECK ([FESTIVO] = 0 or [FESTIVO] = 1)
);


GO

/*  INSERT TRIGGER "TI_CALMACCHINE" FOR TABLE "CALMACCHINE"  */
CREATE TRIGGER TI_CALMACCHINE ON CALMACCHINE FOR INSERT AS
BEGIN
    DECLARE
       @MAXCARD  INT,
       @NUMROWS  INT,
       @NUMNULL  INT,
       @ERRNO    INT,
       @ERRMSG   VARCHAR(255)

    SELECT  @NUMROWS = @@ROWCOUNT
    IF @NUMROWS = 0
       RETURN

    
    /*  PARENT "TABELLAMACCHINE" MUST EXIST WHEN INSERTING A CHILD IN "CALMACCHINE"  */
    IF UPDATE(CODMAC)
    BEGIN
       IF (SELECT COUNT(*)
           FROM   TABELLAMACCHINE T1, INSERTED T2
           WHERE  T1.CODICE = T2.CODMAC) != @NUMROWS
          BEGIN
             SELECT @ERRNO  = 30002,
                    @ERRMSG = 'Parent does not exist in "TABELLAMACCHINE". Cannot create child in "CALMACCHINE".'
             GOTO ERROR
          END
    END

    RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO

/*  UPDATE TRIGGER "TU_CALMACCHINE" FOR TABLE "CALMACCHINE"  */
CREATE TRIGGER TU_CALMACCHINE ON CALMACCHINE FOR UPDATE AS
BEGIN
   DECLARE
      @MAXCARD  INT,
      @NUMROWS  INT,
      @NUMNULL  INT,
      @ERRNO    INT,
      @ERRMSG   VARCHAR(255)

      SELECT  @NUMROWS = @@ROWCOUNT
      IF @NUMROWS = 0
         RETURN

      
      /*  PARENT "TABELLAMACCHINE" MUST EXIST WHEN UPDATING A CHILD IN "CALMACCHINE"  */
      IF UPDATE(CODMAC)
      BEGIN
         IF (SELECT COUNT(*)
             FROM   TABELLAMACCHINE T1, INSERTED T2
             WHERE  T1.CODICE = T2.CODMAC) != @NUMROWS
            BEGIN
               SELECT @ERRNO  = 30003,
                      @ERRMSG = '"TABELLAMACCHINE" does not exist. Cannot ALTER COLUMN child in "CALMACCHINE".'
               GOTO ERROR
            END
      END

      RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO
GRANT DELETE
    ON OBJECT::[dbo].[CALMACCHINE] TO [Metodo98]
    AS [dbo];


GO
GRANT INSERT
    ON OBJECT::[dbo].[CALMACCHINE] TO [Metodo98]
    AS [dbo];


GO
GRANT REFERENCES
    ON OBJECT::[dbo].[CALMACCHINE] TO [Metodo98]
    AS [dbo];


GO
GRANT SELECT
    ON OBJECT::[dbo].[CALMACCHINE] TO [Metodo98]
    AS [dbo];


GO
GRANT UPDATE
    ON OBJECT::[dbo].[CALMACCHINE] TO [Metodo98]
    AS [dbo];

