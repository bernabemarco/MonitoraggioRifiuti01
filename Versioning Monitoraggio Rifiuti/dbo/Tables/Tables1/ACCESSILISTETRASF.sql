CREATE TABLE [dbo].[ACCESSILISTETRASF] (
    [CODLISTA]       VARCHAR (3)  NOT NULL,
    [NOMEUTENTE]     VARCHAR (25) NOT NULL,
    [TIPORIGHE]      VARCHAR (3)  NULL,
    [FLAGABILITA]    SMALLINT     DEFAULT (0) NULL,
    [UTENTEMODIFICA] VARCHAR (25) NOT NULL,
    [DATAMODIFICA]   DATETIME     NOT NULL,
    CONSTRAINT [PK_ACCESSILISTETRASF] PRIMARY KEY CLUSTERED ([CODLISTA] ASC, [NOMEUTENTE] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [CKC_FLAGABILITA_ACCESSILISTETR] CHECK ([FLAGABILITA] = 0 or [FLAGABILITA] = 1)
);


GO

/*  UPDATE TRIGGER "TU_ACCESSILISTETRASF" FOR TABLE "ACCESSILISTETRASF"  */
CREATE TRIGGER TU_ACCESSILISTETRASF ON ACCESSILISTETRASF FOR UPDATE AS
BEGIN
   DECLARE
      @MAXCARD  INT,
      @NUMROWS  INT,
      @NUMNULL  INT,
      @ERRNO    INT,
      @ERRMSG   VARCHAR(255)

      SELECT  @NUMROWS = @@ROWCOUNT
      IF @NUMROWS = 0
         RETURN

      
      /*  PARENT "PARAMETRILISTETRASF" MUST EXIST WHEN UPDATING A CHILD IN "ACCESSILISTETRASF"  */
      IF UPDATE(CODLISTA)
      BEGIN
         IF (SELECT COUNT(*)
             FROM   PARAMETRILISTETRASF T1, INSERTED T2
             WHERE  T1.CODICE = T2.CODLISTA) != @NUMROWS
            BEGIN
               SELECT @ERRNO  = 30003,
                      @ERRMSG = '"PARAMETRILISTETRASF" does not exist. Cannot ALTER COLUMN child in "ACCESSILISTETRASF".'
               GOTO ERROR
            END
      END

      RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO

/*  INSERT TRIGGER "TI_ACCESSILISTETRASF" FOR TABLE "ACCESSILISTETRASF"  */
CREATE TRIGGER TI_ACCESSILISTETRASF ON ACCESSILISTETRASF FOR INSERT AS
BEGIN
    DECLARE
       @MAXCARD  INT,
       @NUMROWS  INT,
       @NUMNULL  INT,
       @ERRNO    INT,
       @ERRMSG   VARCHAR(255)

    SELECT  @NUMROWS = @@ROWCOUNT
    IF @NUMROWS = 0
       RETURN

    
    /*  PARENT "PARAMETRILISTETRASF" MUST EXIST WHEN INSERTING A CHILD IN "ACCESSILISTETRASF"  */
    IF UPDATE(CODLISTA)
    BEGIN
       IF (SELECT COUNT(*)
           FROM   PARAMETRILISTETRASF T1, INSERTED T2
           WHERE  T1.CODICE = T2.CODLISTA) != @NUMROWS
          BEGIN
             SELECT @ERRNO  = 30002,
                    @ERRMSG = 'Parent does not exist in "PARAMETRILISTETRASF". Cannot create child in "ACCESSILISTETRASF".'
             GOTO ERROR
          END
    END

    RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO
GRANT DELETE
    ON OBJECT::[dbo].[ACCESSILISTETRASF] TO [Metodo98]
    AS [dbo];


GO
GRANT INSERT
    ON OBJECT::[dbo].[ACCESSILISTETRASF] TO [Metodo98]
    AS [dbo];


GO
GRANT REFERENCES
    ON OBJECT::[dbo].[ACCESSILISTETRASF] TO [Metodo98]
    AS [dbo];


GO
GRANT SELECT
    ON OBJECT::[dbo].[ACCESSILISTETRASF] TO [Metodo98]
    AS [dbo];


GO
GRANT UPDATE
    ON OBJECT::[dbo].[ACCESSILISTETRASF] TO [Metodo98]
    AS [dbo];

