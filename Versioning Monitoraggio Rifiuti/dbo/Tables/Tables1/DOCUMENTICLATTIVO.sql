CREATE TABLE [dbo].[DOCUMENTICLATTIVO] (
    [CODORDINE]      VARCHAR (3)  NOT NULL,
    [CODDOCUMENTO]   VARCHAR (3)  NOT NULL,
    [POSIZIONE]      DECIMAL (5)  NULL,
    [DATAMODIFICA]   DATETIME     NOT NULL,
    [UTENTEMODIFICA] VARCHAR (25) NOT NULL,
    CONSTRAINT [PK_DOCUMENTICLATTIVO] PRIMARY KEY CLUSTERED ([CODORDINE] ASC, [CODDOCUMENTO] ASC) WITH (FILLFACTOR = 90)
);


GO

/*  UPDATE TRIGGER "TU_DOCUMENTICLATTIVO" FOR TABLE "DOCUMENTICLATTIVO"  */
CREATE TRIGGER TU_DOCUMENTICLATTIVO ON DOCUMENTICLATTIVO FOR UPDATE AS
BEGIN
   DECLARE
      @MAXCARD  INT,
      @NUMROWS  INT,
      @NUMNULL  INT,
      @ERRNO    INT,
      @ERRMSG   VARCHAR(255)

      SELECT  @NUMROWS = @@ROWCOUNT
      IF @NUMROWS = 0
         RETURN

      
      /*  PARENT "PARAMETRIORDPROD" MUST EXIST WHEN UPDATING A CHILD IN "DOCUMENTICLATTIVO"  */
      IF UPDATE(CODORDINE)
      BEGIN
         IF (SELECT COUNT(*)
             FROM   PARAMETRIORDPROD T1, INSERTED T2
             WHERE  T1.CODICE = T2.CODORDINE) != @NUMROWS
            BEGIN
               SELECT @ERRNO  = 30003,
                      @ERRMSG = '"PARAMETRIORDPROD" does not exist. Cannot ALTER COLUMN child in "DOCUMENTICLATTIVO".'
               GOTO ERROR
            END
      END

      RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO

/*  INSERT TRIGGER "TI_DOCUMENTICLATTIVO" FOR TABLE "DOCUMENTICLATTIVO"  */
CREATE TRIGGER TI_DOCUMENTICLATTIVO ON DOCUMENTICLATTIVO FOR INSERT AS
BEGIN
    DECLARE
       @MAXCARD  INT,
       @NUMROWS  INT,
       @NUMNULL  INT,
       @ERRNO    INT,
       @ERRMSG   VARCHAR(255)

    SELECT  @NUMROWS = @@ROWCOUNT
    IF @NUMROWS = 0
       RETURN

    
    /*  PARENT "PARAMETRIORDPROD" MUST EXIST WHEN INSERTING A CHILD IN "DOCUMENTICLATTIVO"  */
    IF UPDATE(CODORDINE)
    BEGIN
       IF (SELECT COUNT(*)
           FROM   PARAMETRIORDPROD T1, INSERTED T2
           WHERE  T1.CODICE = T2.CODORDINE) != @NUMROWS
          BEGIN
             SELECT @ERRNO  = 30002,
                    @ERRMSG = 'Parent does not exist in "PARAMETRIORDPROD". Cannot create child in "DOCUMENTICLATTIVO".'
             GOTO ERROR
          END
    END

    RETURN

/*  ERRORS HANDLING  */
ERROR:
    RAISERROR (@ERRMSG, 1, 1)
    ROLLBACK  TRANSACTION
END

GO
GRANT DELETE
    ON OBJECT::[dbo].[DOCUMENTICLATTIVO] TO [Metodo98]
    AS [dbo];


GO
GRANT INSERT
    ON OBJECT::[dbo].[DOCUMENTICLATTIVO] TO [Metodo98]
    AS [dbo];


GO
GRANT REFERENCES
    ON OBJECT::[dbo].[DOCUMENTICLATTIVO] TO [Metodo98]
    AS [dbo];


GO
GRANT SELECT
    ON OBJECT::[dbo].[DOCUMENTICLATTIVO] TO [Metodo98]
    AS [dbo];


GO
GRANT UPDATE
    ON OBJECT::[dbo].[DOCUMENTICLATTIVO] TO [Metodo98]
    AS [dbo];

