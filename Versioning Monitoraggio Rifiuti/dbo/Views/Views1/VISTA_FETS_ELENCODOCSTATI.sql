
CREATE VIEW [dbo].[VISTA_FETS_ELENCODOCSTATI] AS
SELECT DISTINCT
	FCD.CODICEMET,PD.DESCRIZIONE
FROM 
	FETS_CODIFICHEDOCUMENTI FCD
INNER JOIN
	ParametriDoc PD
On
	PD.Codice = FCD.CodiceMet
GO
GRANT SELECT
    ON OBJECT::[dbo].[VISTA_FETS_ELENCODOCSTATI] TO [Metodo98]
    AS [dbo];

